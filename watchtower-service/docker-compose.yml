version: "3.3"

services:
  db:
    image: mysql:8.0
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: watchtower
      MYSQL_PASSWORD: watchtower
      MYSQL_DATABASE: watchtower