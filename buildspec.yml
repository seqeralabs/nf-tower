version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in the install phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - "./gradlew assemble"
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - watchtower-service/build/libs/watchtower-service-0.1.jar
cache:
  paths:
    - '/root/.m2/**/*'